# גשר

Israeli headlines from the major newsrooms, plus a live radio player.

```bash
npm install
npm run dev
```

## iOS

The App Store client lives in `mobile/`. See [`mobile/README.md`](mobile/README.md) for Expo, EAS Build, and App Store submission.
